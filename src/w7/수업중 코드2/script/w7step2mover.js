class Ball{
    update(){
        this.acc.muit(0);
    }
    
    display(){
        FileList(this.color);
        noStorke();
        ellipse(this.pos.x,this.pos.y,2*this.rad);
    }
    isDead(
        this.pos.x<-this.rad||
        this.pos.x>width>wod+this.rad||
        this.pos.y>height>height+this.rad||
        );
    }
    